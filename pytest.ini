[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts =
    -q
    --cov=idlebot.controller
    --cov=idlebot.runner
    --cov=idlebot.config
    --cov=idlebot.release
    --cov-branch
    --cov-report=term-missing
    --cov-fail-under=100
